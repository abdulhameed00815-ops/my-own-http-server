so, in the next session i would want to do the following:
1-parse the signature of the endpoint function, then make a list of dicts with the name and the type of the params.
2-check the schema of the endpoint url, and map the types of the params made in step 1 with the same param name.
3-inject params with the correct type and order as in the schema, run it, and handle its failure cases.

